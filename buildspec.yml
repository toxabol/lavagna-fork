version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  pre_build:
    commands:
    - echo "Entered the pre-build phase"
    - pwd
  build:
    commands:
    - echo "Entered build phase"
    - mvn -B package --file pom.xml
  post_build:
    commands:
    - echo "Entered the post-build phase"
    - cp /codebuild/output/src133566369/src/conf_scripts/location.properties /codebuild/output/src133566369/src/target/lavagna
#    - mv /target/lavagna ./
    
artifacts:
  files:
    - /target/lavagna/**/*
